name: jarvis_assistant
description: Jarvis Hybrid AI
publish_to: none
version: 3.0.0+3

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  speech_to_text: ^6.6.2
  flutter_tts: ^3.8.5
  connectivity_plus: ^6.0.5

flutter:
  uses-material-design: true
