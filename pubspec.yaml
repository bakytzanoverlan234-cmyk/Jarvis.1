name: jarvis_assistant
description: Jarvis AI Assistant MAX
publish_to: none
version: 2.0.0+2

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  speech_to_text: ^6.6.0
  flutter_tts: ^3.8.5

flutter:
  uses-material-design: true
